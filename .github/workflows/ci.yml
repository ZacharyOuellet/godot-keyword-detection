# For syntax, see https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax
# This workflow is automatically triggered for every push and pull request, to verify that everything still builds.

name: Continuous Integration
on:
  workflow_call:
  push:
    branches:
      - 'main'
  pull_request:
  merge_group:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # Test a few selected combinations of parameters ("test case matrix").
        # While it would be nice to test all needed combinations, in practice this would a waste of CI usage time.
        # By building just with a few combinations, we only need a few runners,
        # while still achieving adequate coverage.
        # To make a full build with all combinations, use the make_build.yml workflow instead.
        include:
          - target: { platform: linux, arch: x86_64, os: ubuntu-22.04 , runner: ["self-hosted", "linux"]}
            target-type: template_debug
            float-precision: single
          - target: { platform: windows, arch: x86_64, os: windows, runner: ["self-hosted", "windows"]}
            target-type: template_release
            float-precision: single
          # - target: { platform: macos, arch: universal, os: macos-latest, runner: macos-latest }
          #   target-type: template_debug
          #   float-precision: single
          - target: { platform: android, arch: arm64, os: ubuntu-22.04, runner: ["self-hosted", "linux"] }
            target-type: template_debug
            float-precision: single
          - target: { platform: web, arch: wasm32, os: ubuntu-22.04, runner: ["self-hosted", "linux"] }
            target-type: template_release
            float-precision: single

    runs-on: ${{ matrix.target.runner }}
    steps:
      # Clone this repository
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Install Linux cross-compilation toolchains
        if: ${{ matrix.target.platform == 'linux' && (matrix.target.arch == 'arm64' || matrix.target.arch == 'arm32') }}
        run: |
          sudo apt-get update && sudo apt-get install -y \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            gcc-arm-linux-gnueabihf g++-arm-linux-gnueabihf

      - name: Install multilib support
        if: ${{ matrix.target.platform == 'linux' && matrix.target.arch == 'x86_32' }}
        run: |
          sudo apt-get update && sudo apt-get install -y gcc-multilib g++-multilib

      - name: Configure cross-compiler shims for Linux ARM64
        if: ${{ matrix.target.platform == 'linux' && matrix.target.arch == 'arm64' }}
        shell: bash
        run: |
          mkdir -p "$HOME/.local/cross-shims"
          ln -sf /usr/bin/aarch64-linux-gnu-gcc  "$HOME/.local/cross-shims/gcc"
          ln -sf /usr/bin/aarch64-linux-gnu-g++  "$HOME/.local/cross-shims/g++"
          ln -sf /usr/bin/aarch64-linux-gnu-ar   "$HOME/.local/cross-shims/ar"
          ln -sf /usr/bin/aarch64-linux-gnu-strip "$HOME/.local/cross-shims/strip"
          echo "$HOME/.local/cross-shims" >> "$GITHUB_PATH"


      # Setup dependencies
      - name: Setup godot-cpp
        uses: ./godot-cpp/.github/actions/setup-godot-cpp
        with:
          platform: ${{ matrix.target.platform }}
          em-version: 3.1.62
          windows-compiler: msvc

      - name: Fix python3 for emcc on self-hosted
        if: ${{ matrix.target.platform == 'web' && contains(join(matrix.target.runner, ','), 'self-hosted') }}
        shell: bash
        run: |
          mkdir -p "$HOME/.local/shims"
          ln -sf /usr/bin/python3 "$HOME/.local/shims/python3"
          echo "$HOME/.local/shims" >> "$GITHUB_PATH"

      - name: Configure cross-compiler for Linux ARM64
        if: ${{ matrix.target.platform == 'linux' && matrix.target.arch == 'arm64' }}
        shell: bash
        run: |
          echo "SCONS_EXTRA=CC=aarch64-linux-gnu-gcc CXX=aarch64-linux-gnu-g++ LINK=aarch64-linux-gnu-g++" >> $GITHUB_ENV

      # Build GDExtension (with caches)

      - name: Restore .scons_cache
        uses: ./godot-cpp/.github/actions/godot-cache-restore
        with:
          scons-cache: ${{ github.workspace }}/.scons-cache/
          cache-name: ${{ matrix.target.platform }}_${{ matrix.target.arch }}_${{ matrix.float-precision }}_${{ matrix.target-type }}

      - name: Build GDExtension Debug Build
        shell: sh
        env:
          SCONS_CACHE: ${{ github.workspace }}/.scons-cache/
        run: |
          scons target=${{ matrix.target-type }} platform=${{ matrix.target.platform }} arch=${{ matrix.target.arch }} precision=${{ matrix.float-precision }}

      - name: Save .scons_cache
        uses: ./godot-cpp/.github/actions/godot-cache-save
        with:
          scons-cache: ${{ github.workspace }}/.scons-cache/
          cache-name: ${{ matrix.target.platform }}_${{ matrix.target.arch }}_${{ matrix.float-precision }}_${{ matrix.target-type }}
